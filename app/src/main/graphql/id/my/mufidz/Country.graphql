query Country($country_code: ID!) {
    country(code: $country_code) {
        code
        emoji
        name
        capital
        languages {
            code
            name
        }
        phone
        states {
            code
            name
        }
        currency
        native
        continent {
            code
            name
        }
    }
}